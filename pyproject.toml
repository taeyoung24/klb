[project]
name = "klb"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv"  
]  

[tool.uv.workspace]
members = ["apps/core-simulator", "apps/backend"]
